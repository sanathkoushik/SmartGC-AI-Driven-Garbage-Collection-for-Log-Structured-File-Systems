# SmartGC Python subsystem dependencies.
#
# Install into a local virtual environment (see docs/reproducibility.md):
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt
#
# PyTorch is used CPU-only; the reference machine for this project has no CUDA
# device. Install the CPU wheel explicitly to avoid pulling a multi-gigabyte
# CUDA runtime:
#   .venv/Scripts/python -m pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.2,<3.0
numpy>=1.26,<3.0
pandas>=2.1,<3.0
matplotlib>=3.8,<4.0
scipy>=1.11,<2.0
scikit-learn>=1.4,<2.0
PyYAML>=6.0,<7.0
tqdm>=4.66,<5.0
requests>=2.31,<3.0
pytest>=8.0,<9.0
